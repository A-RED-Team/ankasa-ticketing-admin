// export const sleep = (time) => new Promise((res) => setTimeout(res, time));

export const sleep = (time) => new Promise((res) => setTimeout(res, time));

// export const setWindowClass = (classList) => {
//   document.body.classList = classList;
//   return () => {
//     document.body.className = '';
//   };
// };

// export const addWindowClass = (className) => {
//   document.body.classList.add(className);
// };

// export const removeWindowClass = (className) => {
//   document.body.classList.remove(className);
// };
